services:
  - name: task-manager-api
    type: web
    env: node
    region: us-west
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
    branch: develop
    autoDeploy: true
    plan: free